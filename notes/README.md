# Notes

Raw working notes the drafted sections were built from — task breakdowns, rough
pipeline sketches, and early framing. Kept for provenance and traceability; these
are inputs, not finished prose.

Two files have a `-v1` sibling that genuinely differs from the main version
(`task-1ca-outline` and `introduction-notes`); both were kept intentionally.
Identical duplicates from the source were de-duplicated.

Note: `task-1b-analyzing.txt` describes the EBM evidence hierarchy backwards —
conventional EBM ranks RCTs and other difference-making evidence *above*
mechanistic reasoning (the EBM+ critique is that it under-weights mechanism).
See the paper drafts for the correct statement.

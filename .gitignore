# --- Secrets: never commit ---
.env
.env.*
*.key
*.pem
secrets.*
**/api_key*
**/*apikey*

# --- Copyrighted reference library (published papers; keep local, do not track) ---
# Remove this line only if the repo is PRIVATE and you want the PDFs tracked.
corpus/reference-library/*.pdf

# --- Copyrighted primary-study PDFs (same policy as the reference library) ---
corpus/primary_studies/*.pdf
# Supplementary appendices/data converted to text for extraction — same copyright
# policy as the PDFs above; keep local, do not track.
corpus/primary_studies/Reynolds2019_SupplementaryAppendix.md

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/
.mypy_cache/

# --- Pipeline run artifacts (uncomment/adjust as the pipeline solidifies) ---
# outputs/
# cache/
# *.log

# --- Internal working/status docs: never meant to be public, kept local only ---
# These track process (what's done, what's open, timelines) rather than the
# paper's actual content. Remove a line here only if you want that specific
# file tracked/public after all.
STATUS.md
docs/
paper/outline-and-timeline.md
# Unwritten section stub — currently just a "not yet written" note plus a
# pointer to a gitignored internal doc (docs/agent-context-pack.md). Remove
# this line once the section actually has drafted content.
paper/03-theoretical-framework.md

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
